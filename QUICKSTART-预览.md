# DevThink 实时预览怎么开（QUICKSTART）

> 一句话：预览地址 `http://127.0.0.1:5180` 不是独立网站，是 DevThink 在「启动预览」时**临时拉起的内部 Vite 开发服务器**。App 没运行、或没点启动，端口就是关的——浏览器直接打开会显示「无法连接」。

---

## 0. 关键前提（先看这条）

| 你现在的状况 | 预览能不能用 |
|------|------|
| 用 **Electron 桌面端** 打开 DevThink（开发模式 `npm run electron:dev` 或装好的 dmg） | ✅ 可以 |
| 在 **浏览器** 里打开 DevThink（localhost:5174 那个 Vite 页面） | ❌ 不行，按钮会提示「需在桌面端运行」 |

> ⚠️ 预览功能依赖桌面端主进程（`runtime.preview` / IPC），**浏览器环境里点「启动预览」不会起服务**。

---

## 1. 标准打开步骤

1. **跑起桌面端**
   - 开发模式：终端 1 `npm run dev`（Vite 5174），终端 2 `npm run electron:dev`（弹出 Electron 窗口）
   - 或已装 dmg：从「应用程序」打开 DevThink
2. 右侧切到 **「💻 代码工程」**，确认能看到已生成的工程（如 `主方案-1`）文件树。
   - 若没有 → 点 **「⚙️ 生成同源全栈工程」**（需左侧先「定稿推送开发窗口」产出设计文档）
3. 右侧切到 **「👁 实时预览」** → 点 **「▶️ 启动预览」**
4. 等 **10~60 秒**（首次会自动 `npm install` 装依赖，之后只需几秒）。看下方**黑色日志区**：
   - 出现 URL（如 `http://127.0.0.1:5180`）即成功，内置 iframe 直接显示页面
   - 报红字 = 启动失败，把日志发同事/助手排查
5. 想单独开浏览器看：点预览条上的 **「↗ 新窗口打开」**，或手动访问 `http://127.0.0.1:5180`。
   - **前提**：该预览会话还活着（App 没关、没点「⏹ 停止预览」）。一旦停止或关 App，端口关闭。

---

## 2. 端口与进程说明

- **5180**：预览前端（Vite dev server），由 Electron 主进程 `preview:start` 拉起，绑定 `127.0.0.1`（IPv4，避免 localhost→IPv6 连不上）。
- **5174**：DevThink 自身的 Vite 开发页（仅开发模式）。
- **3001**：生成工程的 Express 后端（**仅「联调真实数据」模式自动起**，由 `preview:start` 的 fullstack 分支 spawn）。
- 生成工程磁盘位置：`~/Library/Application Support/DevThink/generated/<工程名>/web`

> ⚠️ 预览服务是**长驻子进程**，直到你点「停止预览」或退出 App 才关闭。关窗口 = 端口关闭 = 浏览器再开 5180 就是「无法连接」。

---

## 3. 三种预览模式（选一个再启动）

预览区顶部有三个模式按钮，**运行中选不了，需先「⏹ 停止预览」再切**：

| 模式 | 按钮 | 启动什么 | 适合 |
|------|------|------|------|
| 开发预览 | 🔄 | 仅前端 `vite dev`（HMR，改码自动刷新） | 边做边看 UI |
| 成品预览 | 📦 | 前端先 `vite build` 再 `vite preview`（=生产产物，所见即所得） | 确认最终网页长相（改码需停止重启） |
| **联调真实数据** | 🔗 | 前端 + 后端 **同时起**（Express 3001, node:sqlite 文件库） | 看带真实增删改查数据的系统 |

**联调真实数据（推荐验证完整闭环）：**
1. 选「🔗 联调真实数据」→ 点「▶️ 启动联调预览」
2. DevThink 会**自动**安装后端依赖、启动 Express 后端（端口 3001）、前端 `/api` 代理到 3001
3. 后端**首次启动自动写入示例数据**（每表 3 条），表格不再是空的，能真实增删改查
4. 需工程含 `backend/` 目录：旧工程（如被清理命名的 `-1`）只有 web 没有后端，会提示「重新生成全栈工程」；在左侧重新生成一次即可
5. 后端要求 **Node ≥ 22**（`--experimental-sqlite`），App 已从 login shell 拉到正确 PATH

> 旧版「手动进目录 `npm start` 起后端」的方式已不再需要——选「联调真实数据」模式即一键完成。

---

## 4. 打不开？对照排查

| 现象 | 原因 | 解决 |
|------|------|------|
| 浏览器开 5180 显示「无法连接」 | 预览没启动 / App 已关 | 打开桌面端 → 切「实时预览」→ 点「启动预览」 |
| 点「启动预览」提示「需在桌面端运行」 | 你在浏览器里跑 DevThink | 改用 Electron 桌面端（见步骤 1） |
| 日志区卡在 `npm install` 很久后失败 | 网络/代理问题装不上依赖 | 检查网络；或手动进 web 目录 `npm install` 后再点启动 |
| 日志区报 `EADDRINUSE` / 端口被占 | 上次的预览进程没退干净 | 退出 DevThink 重开，或杀掉占 5180 的进程后重试 |
| iframe 白屏但日志显示已启动 | 旧缓存 / 内部端口未对齐 | 强制刷新（Cmd+Shift+R）；确认用的是最新包（含 IPv4 修复） |

---

## 5. 一句话记忆

> **预览不是网址，是按钮。** 打开 DevThink 桌面端 → 生成工程 → 点「启动预览」→ 看内置窗口（或浏览器 5180，仅会话期间有效）。

---
*本文档随功能迭代维护；若预览入口/端口有变动，以 `src/components/RightPanel.vue`（实时预览标签）与 `electron/main.cjs`（`preview:start` 处理）为准。*
